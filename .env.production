# Production frontend env — committed on purpose: contains NO secrets,
# only the public backend URL (it ends up in the client bundle anyway).
# Local/dev builds use .env (gitignored) instead.
VITE_BACKEND_URL=https://api.medik.us.ci
